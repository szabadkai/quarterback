{
  "name": "quarterback",
  "version": "1.0.23",
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.86.2",
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.4",
    "lz-string": "^1.5.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "node --test && playwright test",
    "test:e2e": "playwright test",
    "test:coverage": "node --test --experimental-test-coverage",
    "prepare": "node scripts/setup-hooks.js",
    "version:auto": "node scripts/version-auto.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "gh-pages": "^6.0.0",
    "vite": "^7.2.6"
  }
}
